export const proficencyaction = (type, tag) => {
    return {
        type,
        tag,
    }
}

export const loginuserinfoaction = (type, userinfoObj) => {
    return {
        type,
        userinfoObj
    }
}

export const userloginaction = (type) => {
    return {
        type
    }
}